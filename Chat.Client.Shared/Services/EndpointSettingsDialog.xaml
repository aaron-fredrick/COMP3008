<Window x:Class="Chat.Client.Shared.Services.EndpointSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Chat.Client.Shared.Controls"
        Title="Connection Settings" Height="360" Width="420"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource WindowBorderBrush}"
            BorderThickness="{StaticResource WindowBorderThickness}"
            CornerRadius="{StaticResource WindowCornerRadius}">
        <Border.Effect>
            <DropShadowEffect Color="{StaticResource WindowShadowColor}"
                              BlurRadius="{StaticResource WindowShadowDepth}"
                              ShadowDepth="{StaticResource WindowShadowDepth}"
                              Direction="270"
                              Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <controls:CustomTitleBar Grid.Row="0"/>

            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="30,24,30,24">
                    <TextBlock x:Name="HostLabel" Text="Server Host" Margin="0,0,0,6" Foreground="{StaticResource ForegroundBrush}" FontSize="13" FontWeight="Medium"/>
                    <TextBox x:Name="HostTextBox" FontSize="13" Padding="8,6" Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}" BorderBrush="{StaticResource BorderBrush}"/>

                    <TextBlock x:Name="PollingPortLabel" Text="Polling Port" Margin="0,16,0,6" Foreground="{StaticResource ForegroundBrush}" FontSize="13" FontWeight="Medium"/>
                    <TextBox x:Name="PollingPortTextBox" FontSize="13" Padding="8,6" Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}" BorderBrush="{StaticResource BorderBrush}"/>

                    <TextBlock x:Name="DuplexPortLabel" Text="Duplex Port" Margin="0,16,0,6" Foreground="{StaticResource ForegroundBrush}" FontSize="13" FontWeight="Medium"/>
                    <TextBox x:Name="DuplexPortTextBox" FontSize="13" Padding="8,6" Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}" BorderBrush="{StaticResource BorderBrush}"/>

                    <TextBlock x:Name="PollingIntervalLabel" Text="Polling Refresh (ms)" Margin="0,16,0,6" Foreground="{StaticResource ForegroundBrush}" FontSize="13" FontWeight="Medium"/>
                    <TextBox x:Name="PollingIntervalTextBox" FontSize="13" Padding="8,6" Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}" BorderBrush="{StaticResource BorderBrush}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,28,0,0">
                        <Button Content="Cancel" Width="85" Margin="0,0,10,0" Padding="0,8" Click="CancelButton_Click" Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>
                        <Button Content="Apply" Width="85" Padding="0,8" Click="SaveButton_Click" Background="{StaticResource AccentBrush}" Foreground="{StaticResource ForegroundBrush}" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
