<Window x:Class="Chat.Client.Duplex.Views.PrivateMessageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Private Message (Duplex)" Height="400" Width="500"
        Background="{StaticResource BackgroundBrush}" WindowStartupLocation="CenterOwner">
    <Grid Margin="{StaticResource MarginNormal}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" x:Name="RecipientText"
                   Text="Private Conversation with: User"
                   Foreground="{StaticResource ForegroundBrush}" FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold"
                   Margin="{StaticResource MarginBottomNormal}"/>

        <!-- Messages -->
        <ListBox Grid.Row="1" x:Name="MessagesListBox"
                 Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}"
                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                 Margin="{StaticResource MarginBottomNormal}"/>

        <!-- Input -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="MessageTextBox"
                     Background="{StaticResource SecondaryBackgroundBrush}" Foreground="{StaticResource ForegroundBrush}"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                     Padding="{StaticResource PaddingNormal}" Margin="{StaticResource MarginRightNormal}"/>

            <Button Grid.Column="1" x:Name="SendButton" Content="Send"
                    Background="{StaticResource AccentBrush}" Foreground="{StaticResource ForegroundBrush}"
                    BorderThickness="0" Padding="{StaticResource ButtonPaddingLarge}"
                    Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Window>
